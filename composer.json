{
  "name": "abc/remote-media-sync",
  "description": "ABC Remote Media Sync",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "authors": [
      {
        "name": "bromate",
        "email": "cedric@moriskelly.com"
      }
  ],
  "repositories": [
    {
      "type": "composer",
      "url": "https://wpackagist.org"
    }
  ],
  "require": {
    "php": ">=7.4"
  },
  "config": {
    "allow-plugins": {
      "composer/installers": true,
      "dealerdirect/phpcodesniffer-composer-installer": true
    },
    "platform": {
      "php": "7.4.33"
    },
    "sort-packages": true
  },
  "require-dev": {
    "squizlabs/php_codesniffer": "^3.13",
    "wp-coding-standards/wpcs": "^3.2"
  },
  "scripts": {
      "lint": "phpcs ./ --standard=WordPress --extensions=php",
      "lint:file": "phpcs --standard=WordPress --extensions=php",
      "fix": "phpcbf --standard=WordPress --extensions=php ./",
      "fix:file": "phpcbf --standard=WordPress --extensions=php",
      "zip": "rm -f abc-remote-media-sync.zip && zip -r abc-remote-media-sync.zip . -x '*.git*' -x 'vendor/*' -x 'composer.json' -x 'composer.lock' -x '*.md'"
  }
  
}
